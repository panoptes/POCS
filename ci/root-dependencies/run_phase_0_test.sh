#!/bin/bash

echo "
       LANG = ${LANG}
     LC_ALL = ${LC_ALL}
        ENV = ${ENV}
      SHELL = ${SHELL}
     PANDIR = ${PANDIR}
     PANLOG = ${PANLOG}
       POCS = ${POCS}
       PAWS = ${PAWS}
    PANUSER = ${PANUSER}
SOLVE_FIELD = ${SOLVE_FIELD}
"