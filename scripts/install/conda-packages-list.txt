# These are packages that we install with conda. They include both Python
# packages and non-Python libraries on which we depend (usually via a
# Python package depending on some native code).

astroplan
astropy >= 3.0.0
ccdproc
cfitsio   # This is an experiment. Requires adjusting apt-packages-list.
codecov
coveralls
cython
dateparser
google-cloud-sdk
google-cloud-storage
matplotlib >= 2.0.0
numpy >= 1.12.1
pycodestyle == 2.3.1
pymongo >= 3.2.2
pyserial >= 3.1.1
pytest >= 3.4.0
pandas
python
python-dateutil >= 2.5.3
PyYAML >= 3.11
pyzmq >= 15.3.0
readline
requests
scikit-image >= 0.12.3
scipy >= 0.17.1

# Jupyter-notebook requires Tornado versions between 4 and 5. See:
# https://stackoverflow.com/questions/49141525/install-jupyter-notebook-on-miniconda
# https://github.com/ipython/ipython/issues/8249
# https://github.com/jupyter/help/issues/324

tornado == 4.5.3
tweepy
wcsaxes
