channels:
  - https://conda.anaconda.org/conda-forge
dependencies:
  - matplotlib-base
  - numpy
  - readline  # POCS shell
  - scipy
  - pip
  - pip:
      - panoptes-utils[config,images,social] >= 0.2.30
